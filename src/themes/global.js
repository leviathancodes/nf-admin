import styled, { createGlobalStyle } from 'styled-components';

const GlobalStyle = createGlobalStyle`
@import url('https://use.typekit.net/roh8omm.css);

  body {
    font-family: futura-pt, sans-serif;
    font-weight: 500;
    font-style: normal;
  }

`;

export default GlobalStyle;
